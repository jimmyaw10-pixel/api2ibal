{
  "name": "ibal-api-consulta",
  "version": "1.0.0",
  "private": true,
  "description": "API Railway: consulta factura IBAL por matrícula (JSON completo de las 4 tarjetas)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:parse": "node parse-ibal.js",
    "postinstall": "npx playwright install chromium || true"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.50.1"
  }
}
